#include "systemc-ams.h"
#include "config.h"

SCA_TDF_MODULE (cti)
{

sca_tdf::sca_in<double> Imcu, Irf; {% for sensorID in activeSensors %}
sca_tdf::sca_in<double>  Isensor{{sensorID}};{% endfor %}
sca_tdf::sca_out<double>  Ptotal; // Compute the total power consumption for battery module

sca_tdf::sca_in<double> Ipv;

SCA_CTOR(cti): Ptotal("Ptotal"), {% for sensorID in activeSensors %}Isensor{{sensorID}}("Isensor{{sensorID}}"), {% endfor %}Imcu("Imcu"), Irf("Irf"), Ipv("Ipv"){};


void set_attributes();

void initialize();

void processing();

};